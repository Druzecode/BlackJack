import sys
import os
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from DispSm import DispSm


def test_show_intro(monkeypatch, input):
    display = DispSm()
    display.show_intro()
    # No assertions, just ensure it runs without error
